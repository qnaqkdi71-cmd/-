# 상세페이지 생성기 의존성

# ── 코어 (이미지 처리 · 키 로딩) ──
Pillow>=10.0.0          # 이미지 스티칭/합성(compose·stitch·export_pdf)
requests>=2.28.0        # HTTP
python-dotenv>=1.0.0    # .env 에서 API 키 자동 로드

# ── 이미지 생성 (Gemini) ──
google-genai>=0.3       # Gemini 이미지 생성(scripts/gemini_api.py)

# ── 렌더링 (HTML → PNG, 한글 카피 합성) ──
playwright>=1.40        # Chromium 캡처
jinja2>=3.1             # 템플릿
pydantic>=2.5           # 데이터 계약(contracts)
