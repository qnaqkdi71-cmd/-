# 레퍼럴 수익 원화 환산 보고서 — 로컬 실행 방법

`referral_krw_report.py` 는 비트겟·OKX 레퍼럴 수수료 수익을 **수익 발생 당일의 국내 거래소
KRW-USDT 종가**로 환산해 PDF 보고서와 CSV 계산내역을 생성합니다.

> **이 저장소에는 완성된 보고서 PDF/CSV가 없습니다.** 작업 환경의 아웃바운드 정책이
> `api.upbit.com` 과 `api.bithumb.com` 을 모두 차단(CONNECT 403)해 종가를 받지 못했고,
> 값을 추정·보간하지 않고 중단했기 때문입니다. 아래 절차대로 국내망 PC에서 실행하면
> 종가 조회부터 PDF 생성까지 한 번에 끝납니다.

---

## 1. 준비

Python 3.9 이상이 필요합니다.

```bash
pip install requests reportlab xlrd
```

| 패키지 | 용도 |
|---|---|
| `requests` | 업비트/빗썸 종가 조회 |
| `reportlab` | PDF 생성 |
| `xlrd` | 비트겟 `.xls` 원본 교차 검증 (생략 가능하나 권장) |

### 한글 폰트

PDF 한글 깨짐을 막기 위해 폰트를 명시적으로 등록합니다. 아래 중 하나만 있으면 됩니다.

- **Windows** — 맑은 고딕(`C:/Windows/Fonts/malgun.ttf`)이 기본 설치되어 있어 추가 작업 불필요
- **macOS** — Apple SD 고딕Neo 기본 탑재. 나눔고딕을 쓰려면 `brew install --cask font-nanum-gothic`
- **Linux** — `sudo apt-get install -y fonts-nanum`
- 위가 모두 안 되면 `NanumGothic.ttf` 를 스크립트와 **같은 디렉터리**에 두면 됩니다

폰트를 못 찾으면 깨진 PDF를 만드는 대신 오류를 내고 중단합니다.

---

## 2. 실행

가장 권장하는 형태 — 거래소 원본 파일을 함께 넘겨 **원본 대조 검증까지** 수행합니다.

```bash
python3 referral_krw_report.py \
  --bitget-xls "Export_spot_transactions20260806_14_51_10.647.xls" \
  --okx-csv    "OKX_Funding_History_2026070520260804UTC....csv" \
  --outdir     "."
```

원본 파일 없이 문서 내 집계표만으로 실행해도 됩니다.

```bash
python3 referral_krw_report.py
```

### 옵션

| 옵션 | 설명 |
|---|---|
| `--outdir DIR` | 산출물 디렉터리 (기본 현재 폴더) |
| `--bitget-xls PATH` | 비트겟 원본 xls. 행 수·일자별 수량을 문서 표와 대조 |
| `--okx-csv PATH` | OKX 원본 csv. 행 수·유형별 건수·일자별 수량을 대조 |
| `--closes-file PATH` | 종가 수동 공급 CSV (`YYYY-MM-DD,종가` 한 줄씩). API 차단 환경용 |
| `--closes-source "..."` | `--closes-file` 사용 시 **필수**. 출처가 보고서 표지에 그대로 기재됩니다 |

---

## 3. 산출물

| 파일 | 내용 |
|---|---|
| `레퍼럴수익_원화환산_보고서.pdf` | 표지 / 한 눈에 보기 / 거래소별 일별 명세 / 일자별 통합 요약 / 참고(본인 수수료 환급) / 미상장 코인 / 고지 사항 / 부록(검증 결과) |
| `레퍼럴수익_계산내역.csv` | 날짜, 거래소, 구분, 코인, 수량, 종가, 원화액 (UTF-8 BOM, 엑셀에서 바로 열림) |

콘솔에는 확정 구간 / 종가 소스 / 비트겟 소계 / OKX 소계 / 총 레퍼럴 수익액 / 참고 합계 /
검증 PASS 개수 / 역산 평균단가 2개가 요약 출력됩니다.

### 종료 코드

| 코드 | 의미 |
|---|---|
| `0` | 정상 완료, 검증 전항목 PASS |
| `1` | 산출물은 생성됐으나 **검증 FAIL 항목 있음** — 부록의 FAIL 항목을 반드시 확인 |
| `2` | **종가 조회 실패로 중단.** 산출물 없음 (값을 지어내지 않음) |

---

## 4. 설계 원칙

- **시차** — 비트겟 원본은 UTC(+9h), OKX 원본은 UTC+8(+1h)을 적용해 KST 일자로 귀속.
  비트겟 지급이 매일 UTC 16:00경이라 KST로는 익일 01:00경이 되므로 **비트겟 첫 수령일은
  07-06**이며 07-05 무수령은 누락이 아닙니다.
- **정밀도** — 전부 `decimal.Decimal`. float 곱셈/합산 없음. 반올림은 최종 표시 단계에서만
  (원화 `ROUND_HALF_UP` 원 단위, 수량 소수점 10자리). 총합은 **반올림 전 값의 총합을
  마지막에 한 번** 반올림하며, 일별·소계 반올림 합과의 차이는 각주로 표기합니다.
- **종가 소스** — 업비트 1순위, 실패 시 빗썸으로 폴백. **한 소스로 전 구간**을 채우며
  날짜별로 섞지 않습니다. 전부 실패하면 추정 없이 중단합니다.
- **소득 분류** — 비트겟 `Rebate rewards` + OKX `Affiliate commission` 만 본 합계.
  비트겟 `Rebate`, OKX `Fee rebate` 는 본인 수수료 환급이라 별도 섹션(합계 제외).
  OKX `Withdrawal`, `From unified trading account` 는 이체·출금이라 완전 제외.
- **미상장 코인** — `LUNC`, `LUNA`, `AB`, `U` 는 KRW 마켓 미상장. 대체 시세를 끌어오지 않고
  수량만 표기 + 원화 0원 처리.

---

## 5. 검증 항목

콘솔과 PDF 부록에 항목별 PASS/FAIL로 전부 출력됩니다.

1. **수량 검증** — 기대 합계와 오차 1e-8 이내 일치
2. **원본 교차 검증** — 비트겟 47행 / OKX 470행(Affiliate 465 + Fee rebate 2 + 이체 2 + 출금 1),
   시차 재적용 후 일자별 수량까지 대조
3. **종가 타당성** — 전 구간 종가 테이블 출력, 원달러 실측 참고치(07-05/07-21/07-29) ±3% 대조,
   최소·최대·평균의 1,300~1,650원 범위 확인, 연속 날짜 누락 확인
4. **행 단위 재계산** — 모든 환산 행에서 `수량 x 종가 == 원화액` 독립 재계산
5. **역산 평균단가** — 1,400~1,560원 범위. 벗어나면 종가 매칭이 하루씩 밀렸는지 먼저 의심
6. **종가 없음 검사** — 종가가 0/None인데 원화액이 0이 아니면 즉시 중단

---

## 6. 알려진 사항: OKX `Fee rebate` 수량

작업 지시서에는 `0.0972540000` 으로 적혀 있었으나, OKX 원본 export 의 2026-07-06 두 행
(`0.04827443 + 0.0489801`)을 합하면 **`0.09725453`** 입니다. 차이 5.3e-7 은 허용 오차
1e-8 을 초과합니다.

거래소 원본 export 가 증빙의 근거이므로 **원본값을 채택**했습니다. 이 항목은 참고 섹션
전용으로 **총 레퍼럴 수익액에는 포함되지 않으므로 대표 금액에는 영향이 없습니다.**
차이는 보고서 경고 섹션과 검증 부록에 그대로 남겨 두었습니다.
